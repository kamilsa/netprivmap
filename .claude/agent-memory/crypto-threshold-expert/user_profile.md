---
name: User Profile
description: Ethereum networking privacy researcher, owner of netprivmap project, expects deep cryptographic rigor and precise citations
type: user
---

The user is working on netprivmap — a visual knowledge map of Ethereum's networking privacy design space. They have strong cryptographic background and expect:
- Precise cryptographic terminology (DDH, ROM, AGM, threshold parameters)
- Real paper citations with authors, venue, year
- Rigorous threat modeling (explicit adversary models, formal properties)
- TypeScript-formatted output compatible with src/data/*.ts schemas
- Never edit src/content/ — always src/data/*.ts only
- Run `npm run build` after data changes to verify compilation
